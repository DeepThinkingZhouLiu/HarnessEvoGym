{
  "name": "deepseek-harness-rsi",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Adapter-based recursive self-improvement control plane for Coding and Cowork agents.",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "harness-rsi": "./controller/src/cli.mjs"
  },
  "scripts": {
    "rsi": "node ./controller/src/cli.mjs",
    "check": "find controller/src controller/test docker -name '*.mjs' -print0 | xargs -0 -n1 node --check",
    "test": "node --test ./controller/test/*.test.mjs"
  },
  "dependencies": {
    "yaml": "2.9.0"
  }
}
