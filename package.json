{
  "name": "harness-evo-gym",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pluggable self-evolution platform for agent Harness targets, environments, and recipes.",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "harness-rsi": "./controller/src/cli.mjs"
  },
  "scripts": {
    "rsi": "node ./controller/src/cli.mjs",
    "check": "find controller/src controller/test docker benchmarks/hle-text-math scripts strategies -name '*.mjs' -print0 | xargs -0 -n1 node --check && python3 -c \"compile(open('scripts/download-hle-text-math.py', encoding='utf-8').read(), 'scripts/download-hle-text-math.py', 'exec')\" && bash -n ./scripts/setup-putnambench-runtime.sh ./scripts/setup-hle-runtime.sh",
    "test": "node --test ./controller/test/*.test.mjs"
  },
  "dependencies": {
    "yaml": "2.9.0"
  }
}
