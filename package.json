{
  "name": "deepseek-harness-rsi",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Adapter-based recursive self-improvement control plane for coding agents.",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "harness-rsi": "./controller/src/cli.mjs"
  },
  "scripts": {
    "rsi": "node ./controller/src/cli.mjs",
    "check": "for file in ./controller/src/*.mjs ./benchmarks/hle-text-math/*.mjs ./scripts/*.mjs; do node --check \"$file\"; done && python3 -c \"compile(open('scripts/download-hle-text-math.py', encoding='utf-8').read(), 'scripts/download-hle-text-math.py', 'exec')\" && bash -n ./scripts/setup-putnambench-runtime.sh ./scripts/setup-hle-runtime.sh",
    "test": "node --test ./controller/test/*.test.mjs"
  }
}
